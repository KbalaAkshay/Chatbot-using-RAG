First create a virtual environment named venv:
python -m venv venv

activate the virtual environment:
venv\Scripts\activate

I have initialized the git 
open cmd and then navigate to that respective folder
using cd path
and give the command git init You can see a hidden folder


Add all the libraries in the requirement.txt file:
streamlit